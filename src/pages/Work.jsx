import React from 'react';

const Work = (props) => {
  const { title, description, duration } = props;

  return (
    <div className="work">
      <h2>Work</h2>
    </div>
  );
}

export default Work;